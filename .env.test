# Used by vitest's global setup, which spins up its own ephemeral Postgres
# instance on this port/database — safe to commit, no real secrets, never
# touches the dev database.
DATABASE_URL=postgresql://postgres:password@localhost:54350/shapio_test
APP_SECRET=test-app-secret-not-for-production-use-only
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=test
ENCRYPTION_KEY=111faaaa6f6a7d7151df4cd5200da02bdb06b9d0dd849b967fc6a674b22f4630
